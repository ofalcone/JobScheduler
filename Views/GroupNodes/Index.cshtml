@model IEnumerable<JobScheduler.Models.GroupNode>

@{
    ViewData["Title"] = nameof(GroupNode);
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Group)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Node)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var groupNodeItem in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => groupNodeItem.Group.Desc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => groupNodeItem.Node.Desc)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", groupNodeItem) |
                    @Html.ActionLink("Details", "Details", groupNodeItem) |
                    @Html.ActionLink("Delete", "Delete", groupNodeItem)
                </td>
            </tr>
        }
    </tbody>
</table>
