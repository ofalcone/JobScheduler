@model JobScheduler.ViewModels.GroupNodeViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>GroupNode</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <select asp-for="NewGroupId" class="form-control" asp-items="ViewBag.GroupId"></select>
            <input type="hidden" asp-for="NewGroupId" />

            <select asp-for="NewNodeId" class="form-control" asp-items="ViewBag.NodeId"></select>
            <input type="hidden" asp-for="NewNodeId" />

            <input type="hidden" asp-for="OldGroupId" />
            <input type="hidden" asp-for="OldNodeId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

