@model JobScheduler.ViewModels.UserRoleViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Detail UserRole</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.User.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.User.Email)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role.Name)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { userId = @Model.UserId, roleId = @Model.RoleId }) |
    <a asp-action="Index">Back to List</a>
</div>
