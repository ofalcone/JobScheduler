@model JobScheduler.ViewModels.JobGroupViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>JobGroup</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <select asp-for="OldJobId" class="form-control" asp-items="ViewBag.JobId"></select>
            <select asp-for="OldGroupId" class="form-control" asp-items="ViewBag.GroupId"></select>

            <input type="hidden" asp-for="NewJobId" />
            <input type="hidden" asp-for="NewGroupId" />
            <input type="hidden" asp-for="OldJobId" />
            <input type="hidden" asp-for="OldGroupId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

